# System Constitution v1 — Minimal Example
# This is the bare minimum valid constitution file

spec: sysconst/v1

project:
  id: minimal.app
  name: "Minimal Application"
  versioning:
    strategy: semver
    current: "1.0.0"

structure:
  root: NodeRef(system.root)

domain:
  nodes:
    - kind: System
      id: system.root
      meta:
        title: "Minimal System"
        description: "A minimal System Constitution example"
      spec:
        goals:
          - "Demonstrate minimal valid constitution structure"

history:
  - version: "1.0.0"
    basedOn: null
    changes: []
    migrations: []
    notes: "Initial minimal constitution"
